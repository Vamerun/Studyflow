{
  "name": "studyflow",
  "version": "1.0.0",
  "description": "A focus timer and task tracker for students. No backend, no build step.",
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "test": "node --test"
  },
  "license": "MIT",
  "devDependencies": {
    "eslint": "^10.11.0"
  }
}
